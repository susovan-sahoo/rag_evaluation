[project]
name = "rag_evaluation"
version = "0.1.0"
description = "Evaluation harness for a transcript-based RAG retriever, with optional cross-encoder reranking, using DeepEval metrics."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "deepeval>=4.1.5",
    "langchain-chroma>=1.1.0",
    "langchain-core>=1.5.3",
    "langchain-openai>=1.4.2",
    "langchain-text-splitters>=1.1.2",
    "openai>=2.53.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
    "sentence-transformers>=5.7.0",
]
